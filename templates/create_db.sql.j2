CREATE DATABASE IF NOT EXISTS iVolve;
CREATE USER IF NOT EXISTS '{{ db_user }}'@'localhost' IDENTIFIED BY '{{ db_user_password }}';
GRANT ALL PRIVILEGES ON iVolve.* TO '{{ db_user }}'@'localhost';
FLUSH PRIVILEGES;

